# Example environment variables for local development
# Copy this to .env and fill in your provider credentials. NEVER commit your real .env to git.

# Encryption key (optional): hex-encoded 32 bytes. If not provided, a dev key will be generated in data/.encryption_key
# ENCRYPTION_KEY=0123456789abcdef... (64 hex chars)

# Google / YouTube
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_OAUTH_CONFIG={"token_uri":"https://oauth2.googleapis.com/token"}

# YouTube (alias of Google in some places)
YOUTUBE_CLIENT_ID=your-youtube-client-id
YOUTUBE_CLIENT_SECRET=your-youtube-client-secret

# Twitter (if using OAuth 2.0 or OAuth 1.0a)
TWITTER_CLIENT_ID=your-twitter-client-id
TWITTER_CLIENT_SECRET=your-twitter-client-secret

# TikTok / Instagram
TIKTOK_CLIENT_ID=your-tiktok-client-id
TIKTOK_CLIENT_SECRET=your-tiktok-client-secret
INSTAGRAM_CLIENT_ID=your-instagram-client-id
INSTAGRAM_CLIENT_SECRET=your-instagram-client-secret

# OAuth redirect URI used during local development. Ensure this value matches the redirect URI configured
# with each provider when registering your app. The default Express callback server listens on port 3000.
REDIRECT_URI=http://localhost:3000/oauth/callback
# Example .env for AI Auto Bot
# Copy to .env and fill secrets before running in production.

# ENCRYPTION_KEY must be 32 bytes (64 hex characters). Example:
# ENCRYPTION_KEY=0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef

# OAuth client IDs & secrets (fill for each provider if you use OAuth flows)
# INSTAGRAM_CLIENT_ID=
# INSTAGRAM_CLIENT_SECRET=
# TIKTOK_CLIENT_ID=
# TIKTOK_CLIENT_SECRET=
# YOUTUBE_CLIENT_ID=
# YOUTUBE_CLIENT_SECRET=
# TWITTER_CLIENT_ID=
# TWITTER_CLIENT_SECRET=

# NOTE:
# - Keep this file out of version control. Do NOT commit real secrets.
# - If ENCRYPTION_KEY is missing the app will generate an ephemeral key (development only).
